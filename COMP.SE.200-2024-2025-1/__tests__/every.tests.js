import every from '../src/every.js';

let test1Data = {};

test('words input "" without pattern to return array []', () => {
    expect(get(test1Data)).toBe(test1Result);
});


let test2Data = {};

test('words input "" without pattern to return array []', () => {
    expect(get(test2Data)).toBe(test1Result);
});


let test3Data = {};

test('words input "" without pattern to return array []', () => {
    expect(get(test3Data)).toBe(test1Result);
});


let test4Data = {};

test('words input "" without pattern to return array []', () => {
    expect(get(test4Data)).toBe(test1Result);
});


let test5Data = {};

test('words input "" without pattern to return array []', () => {
    expect(get(test5Data)).toBe(true);
});